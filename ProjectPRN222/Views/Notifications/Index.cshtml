@model IEnumerable<ProjectPRN222.Models.Notification>
@{
    ViewData["Title"] = "Thông báo";
}

<div class="container py-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">🔔 Thông báo</h4>
                </div>
                <div class="card-body p-0">
                    <div class="p-3 border-bottom">
                        <button class="btn btn-sm btn-primary me-2" onclick="MarkAllAsRead()">
                            Đánh dấu tất cả đã đọc
                        </button>
                        <button class="btn btn-sm btn-secondary" onclick="LoadNotifications()">
                            Làm mới
                        </button>
                    </div>
                    
                    <!-- Danh sách notifications -->
                    <div id="notificationsList">
                        <div class="text-center py-5">
                            <p class="text-muted">Đang tải thông báo...</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/notifications.js"></script>
} 