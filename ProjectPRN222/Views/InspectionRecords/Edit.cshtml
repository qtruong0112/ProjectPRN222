@model ProjectPRN222.Models.InspectionRecord

@{
    ViewData["Title"] = "Chỉnh sửa kết quả đăng kiểm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-7">
            <div class="card shadow rounded-4">
                <div class="card-header bg-primary text-white rounded-top-4 d-flex align-items-center">
                    <i class="bi bi-pencil-square me-2"></i>
                    <span class="fs-5 fw-semibold">Chỉnh sửa kết quả đăng kiểm</span>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="RecordId" />
                        <div class="mb-3">
                            <label asp-for="VehicleId" class="form-label fw-semibold"></label>
                            <select asp-for="VehicleId" class="form-select rounded-3" asp-items="ViewBag.VehicleId"></select>
                            <span asp-validation-for="VehicleId" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="StationId" class="form-label fw-semibold"></label>
                            <select asp-for="StationId" class="form-select rounded-3" asp-items="ViewBag.StationId"></select>
                            <span asp-validation-for="StationId" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="InspectorId" class="form-label fw-semibold"></label>
                            <select asp-for="InspectorId" class="form-select rounded-3" asp-items="ViewBag.InspectorId"></select>
                            <span asp-validation-for="InspectorId" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="InspectionDate" class="form-label fw-semibold"></label>
                            <input asp-for="InspectionDate" class="form-control rounded-3" />
                            <span asp-validation-for="InspectionDate" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Result" class="form-label fw-semibold"></label>
                            <input asp-for="Result" class="form-control rounded-3" />
                            <span asp-validation-for="Result" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Co2emission" class="form-label fw-semibold"></label>
                            <input asp-for="Co2emission" class="form-control rounded-3" />
                            <span asp-validation-for="Co2emission" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Hcemission" class="form-label fw-semibold"></label>
                            <input asp-for="Hcemission" class="form-control rounded-3" />
                            <span asp-validation-for="Hcemission" class="text-danger small"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Comments" class="form-label fw-semibold"></label>
                            <input asp-for="Comments" class="form-control rounded-3" />
                            <span asp-validation-for="Comments" class="text-danger small"></span>
                        </div>
                        <div class="d-flex justify-content-end gap-2">
                            <button type="submit" class="btn btn-primary rounded-pill px-4">
                                <i class="bi bi-save me-1"></i> Lưu
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">
                                <i class="bi bi-arrow-left me-1"></i> Quay lại danh sách
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

<!-- Bootstrap Icons CDN (if not already included in _Layout) -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
