@model ProjectPRN222.Models.InspectionAppointment

@{
    ViewData["Title"] = "Chỉnh sửa lịch hẹn đăng kiểm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-7 col-lg-6">
            <div class="card shadow rounded-5 border-0">
                <div class="card-header bg-info text-white rounded-top-5 border-0">
                    <h4 class="mb-0"><i class="bi bi-pencil-square"></i> Chỉnh sửa lịch hẹn đăng kiểm</h4>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit" class="rounded-5">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="AppointmentId" />
                        <div class="mb-3">
                            <label asp-for="VehicleId" class="form-label fw-semibold"></label>
                            <select asp-for="VehicleId" class="form-control rounded-5" asp-items="ViewBag.VehicleId"></select>
                            <span asp-validation-for="VehicleId" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="UserId" class="form-label fw-semibold"></label>
                            <select asp-for="UserId" class="form-control rounded-5" asp-items="ViewBag.UserId"></select>
                            <span asp-validation-for="UserId" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="StationId" class="form-label fw-semibold"></label>
                            <select asp-for="StationId" class="form-control rounded-5" asp-items="ViewBag.StationId"></select>
                            <span asp-validation-for="StationId" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="AppointmentDate" class="form-label fw-semibold"></label>
                            <input asp-for="AppointmentDate" class="form-control rounded-5" />
                            <span asp-validation-for="AppointmentDate" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Status" class="form-label fw-semibold"></label>
                            <input asp-for="Status" class="form-control rounded-5" />
                            <span asp-validation-for="Status" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="mb-3">
                            <label asp-for="Note" class="form-label fw-semibold"></label>
                            <input asp-for="Note" class="form-control rounded-5" />
                            <span asp-validation-for="Note" class="text-danger rounded-3 d-inline-block px-2"></span>
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-info btn-lg rounded-5 fw-bold shadow-sm text-white">
                                <i class="bi bi-save"></i> Lưu thay đổi
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="text-center mt-4">
                <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4 py-2 fw-semibold shadow-sm back-btn">
                    <i class="bi bi-arrow-left"></i> Quay lại danh sách
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap Icons CDN (nếu chưa có) -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<style>
    .back-btn:hover {
        background: #e9ecef;
        color: #495057 !important;
        border-color: #adb5bd;
    }
</style>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
